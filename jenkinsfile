pipeline {
    agent any

    stages {
        stage('NPM Install') {
            steps {
                sh 'npm install'
            }
        }
        stage('Run Integreation Test') {
            steps {
                sh 'npm test'
            }
        }
    }
}
